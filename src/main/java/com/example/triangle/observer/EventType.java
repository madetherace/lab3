package com.example.triangle.observer;

/**
 * Defines the types of events that can occur in the repository.
 */
public enum EventType {
    ADD,
    REMOVE // Добавим на случай, если понадобится удаление
    // UPDATE // Если бы треугольники были изменяемыми
}